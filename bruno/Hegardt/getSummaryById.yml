info:
  name: getSummaryById
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/persons/getSummaryById/619"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
