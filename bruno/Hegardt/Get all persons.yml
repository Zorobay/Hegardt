info:
  name: Get all persons
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/persons/getAll"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
